# to pass parameters/variables to dvc.yaml like train-test-split ratio, bi-gram, tri-gram, uni-gram, max_depth and other hyperparameters.

data_ingestion:
  test_size: 0.21

model_building:
  ngram_range: [1, 3]  
  max_features: 10000
  learning_rate: 0.09
  max_depth: 20
  n_estimators: 367